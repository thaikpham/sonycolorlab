# colorlab-backend/wrangler.toml
name = "sonycolorlab-api" # Tên Worker của bạn, có thể thay đổi
main = "worker.js"
compatibility_date = "2024-03-20"

[vars]
# Chúng ta sẽ thêm secrets bằng dòng lệnh, không đặt ở đây
