name = "sonycolorlab"
type = "pages"

# Optional fields:
account_id = "your-account-id"  # Optional if deploying from Cloudflare UI
project_name = "sonycolorlab"

# Where Vite outputs the built site
pages_build_output_dir = "dist"
